# SketchGuess Environment Configuration
# Copy this file to .env and customize the values

# Web Application Settings
PORT=3000
NODE_ENV=development

# WebSocket Server Settings
WS_PORT=8080
WS_HOST=0.0.0.0  # Use 0.0.0.0 to listen on all interfaces

# Frontend to WebSocket Connection
# For development, use ws://
# For production with SSL, use wss://
WEBSOCKET_URL=ws://localhost:8080

# Game Settings (optional, can be changed in UI)
DEFAULT_MAX_ROUNDS=5
DEFAULT_TIME_PER_ROUND=60

# Session Security (optional)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#SESSION_SECRET=

# Database Settings (if implementing persistence)
#DB_HOST=localhost
#DB_PORT=27017
#DB_NAME=sketchguess
#DB_USER=
#DB_PASSWORD= 